Every function have it's own file, sorted in categories(folders)
The file "Lobby.py" is the "main" (call logic of indicators)
The folder "Debug" contain program that i've create to automatize the task(IfMissingFile.py: List Missing file and file that are in the wrong folder, AutoCONFIG.py: is a file that add automatically the CONFIG dic to every file)
